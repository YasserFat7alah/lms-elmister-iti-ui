# تشغيل المشروع مع WebSocket

## الخطوة 1: تشغيل خادم WebSocket

في terminal جديد، قم بتشغيل:

```bash
node server/websocket.js
```

يجب أن ترى الرسالة:
```
🚀 WebSocket server running on port 3001
```

## الخطوة 2: تشغيل تطبيق Next.js

في terminal آخر، قم بتشغيل:

```bash
npm run dev
```

## الخطوة 3: فتح الفصل الدراسي

1. افتح المتصفح على `http://localhost:3000`
2. انتقل إلى صفحة الفصل الدراسي
3. افتح نافذة متصفح أخرى (أو وضع التصفح المتخفي) على نفس الرابط
4. الآن يمكنك رؤية التحديثات الفورية بين النوافذ!

## الميزات المتاحة

### ✅ المشاركون في الوقت الفعلي
- رؤية جميع المستخدمين المتصلين
- تحديث فوري عند انضمام أو مغادرة مستخدم

### ✅ الدردشة الفورية
- إرسال واستقبال الرسائل فوراً
- رؤية من يكتب
- تمييز رسائل المعلم

### ✅ السبورة التفاعلية
- الرسم المشترك في الوقت الفعلي
- رؤية رسومات المستخدمين الآخرين فوراً
- أدوات رسم متعددة

### ✅ حالة الوسائط
- معرفة حالة الكاميرا والميكروفون لكل مستخدم
- تحديثات فورية عند تغيير الحالة

## ملاحظات مهمة

- تأكد من تشغيل خادم WebSocket قبل تشغيل التطبيق
- يتم إنشاء مستخدم فريد تلقائياً لكل جلسة
- البيانات مخزنة في الذاكرة (ستفقد عند إعادة تشغيل الخادم)
- للاختبار، افتح عدة نوافذ متصفح

## استكشاف الأخطاء

### إذا لم يتصل WebSocket:
1. تأكد من تشغيل خادم WebSocket على المنفذ 3001
2. تحقق من عدم وجود برنامج آخر يستخدم المنفذ 3001
3. تحقق من console المتصفح للأخطاء

### إذا لم تظهر التحديثات:
1. افتح console المتصفح
2. ابحث عن رسائل "Connected to WebSocket server"
3. تأكد من أن المستخدمين في نفس الغرفة (classId)
